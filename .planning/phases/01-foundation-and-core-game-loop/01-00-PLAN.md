---
phase: 01-foundation-and-core-game-loop
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - "Math Quiz Tests/GameViewModelTests.swift"
  - "Math Quiz Tests/QuestionGeneratorTests.swift"
  - "Math Quiz Tests/SoundPlayerTests.swift"
  - "Math Quiz Tests/SchemaTests.swift"
  - "Math Quiz UITests/NumberPadUITests.swift"
  - "Math Quiz UITests/GameViewUITests.swift"
autonomous: true
requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04
  - GAME-05
  - GAME-06
  - GAME-07
  - GAME-08
  - GAME-09
  - TECH-02

must_haves:
  truths:
    - "All test stub files compile without errors"
    - "Every test function is marked @Test (Swift Testing) and calls Issue.record() or XCTFail() so tests are red/failing until implementation ships"
    - "Test targets resolve correctly after SpriteKit deletion"
  artifacts:
    - path: "Math Quiz Tests/GameViewModelTests.swift"
      provides: "Failing stubs for GAME-01, GAME-02, GAME-05, GAME-07, GAME-09"
    - path: "Math Quiz Tests/QuestionGeneratorTests.swift"
      provides: "Failing stubs for GAME-02 question validity + division-by-zero guard"
    - path: "Math Quiz Tests/SoundPlayerTests.swift"
      provides: "Failing stubs for GAME-06 crash-free playback"
    - path: "Math Quiz Tests/SchemaTests.swift"
      provides: "Failing stub for TECH-02 ModelContainer initialization"
    - path: "Math Quiz UITests/NumberPadUITests.swift"
      provides: "Failing stubs for GAME-03/GAME-04 button count and minimum size"
    - path: "Math Quiz UITests/GameViewUITests.swift"
      provides: "Failing stubs for GAME-08 score visibility"
  key_links:
    - from: "Math Quiz Tests/GameViewModelTests.swift"
      to: "Math Quiz/ViewModels/GameViewModel.swift"
      via: "@testable import"
      pattern: "@testable import"
    - from: "Math Quiz Tests/SchemaTests.swift"
      to: "Math Quiz/Models/Schema/SchemaV1.swift"
      via: "@testable import"
      pattern: "testModelContainerInitialization"
---

<objective>
Create all test stub files that later implementation plans will make green. Stubs must compile and fail (red), giving the Nyquist TDD cycle a valid starting state before any production code is written.

Purpose: Nyquist validation requires every automated verify command to reference a file that exists. Wave 0 creates those files so no implementation plan runs against a MISSING stub.
Output: Six test files (four unit, two UI) with failing @Test / XCTest stubs for all Phase 1 requirements.
</objective>

<execution_context>
@/Users/adam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/adam/dev/Math Quiz/.planning/ROADMAP.md
@/Users/adam/dev/Math Quiz/.planning/phases/01-foundation-and-core-game-loop/01-VALIDATION.md
@/Users/adam/dev/Math Quiz/.planning/phases/01-foundation-and-core-game-loop/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit test stubs (GameViewModelTests, QuestionGeneratorTests, SoundPlayerTests, SchemaTests)</name>
  <files>Math Quiz Tests/GameViewModelTests.swift, Math Quiz Tests/QuestionGeneratorTests.swift, Math Quiz Tests/SoundPlayerTests.swift, Math Quiz Tests/SchemaTests.swift</files>
  <behavior>
    - GameViewModelTests:
      - testOperationSelection: stub, fails with Issue.record("not implemented")
      - testSessionProducesQuestion: stub, fails with Issue.record("not implemented")
      - testFeedbackStateTransition: stub, fails with Issue.record("not implemented")
      - testElapsedSecondsIncrement: stub, fails with Issue.record("not implemented")
      - testSpeedBonusPoints: stub, fails with Issue.record("not implemented")
    - QuestionGeneratorTests:
      - testQuestionIsValid: stub, fails
      - testNoDivisionByZero: stub, fails
    - SoundPlayerTests:
      - testPlayCorrectDoesNotCrash: stub, fails
      - testPlayIncorrectDoesNotCrash: stub, fails
    - SchemaTests:
      - testModelContainerInitialization: stub, fails
  </behavior>
  <action>
    Write four Swift Testing test files into the "Math Quiz Tests" group. Each file uses `import Testing` and `@testable import MathQuiz` (the module name matches the app target). Each test function is decorated with `@Test` and its body calls `Issue.record("not implemented — Wave 0 stub")` so the test registers as a failure (red) rather than a skip or pass.

    IMPORTANT: Do NOT import SwiftUI in any test file. GameViewModel must be testable with only Foundation + Observation imports.

    For SchemaTests, also `import SwiftData` since ModelContainer is SwiftData API.

    The test target name is "Math Quiz Tests" (with space). Verify with:
    `xcodebuild test -scheme "Math Quiz" -destination "platform=iOS Simulator,name=iPhone 16" -only-testing "Math QuizTests" 2>&1 | grep -E "error:|FAILED|passed|failed" | head -30`

    If the test target does not yet exist in the project (SpriteKit template may not have created one), create the target via the Xcode project file — but prefer using xcodebuild to confirm it builds first. If the target is missing, note it in the task summary so Plan 01 can add it during project setup.

    File content pattern for each stub file:
    ```swift
    import Testing
    @testable import MathQuiz   // or the actual module name

    @Suite("GameViewModel Tests")
    struct GameViewModelTests {
        @Test func testOperationSelection() {
            Issue.record("not implemented — Wave 0 stub")
        }
        // ... additional stubs
    }
    ```
  </action>
  <verify>
    <automated>xcodebuild test -scheme "Math Quiz" -destination "platform=iOS Simulator,name=iPhone 16" -only-testing "Math QuizTests" 2>&1 | grep -E "error:|Build FAILED|Test Suite" | head -20</automated>
  </verify>
  <done>All four unit test files exist and the test scheme compiles. Tests are red (failing stubs), not compiler errors. Build succeeds even if tests fail.</done>
</task>

<task type="auto">
  <name>Task 2: Create UI test stubs (NumberPadUITests, GameViewUITests)</name>
  <files>Math Quiz UITests/NumberPadUITests.swift, Math Quiz UITests/GameViewUITests.swift</files>
  <action>
    Write two XCTest UI test files into the "Math Quiz UITests" group. UI tests use XCTest (not Swift Testing) because Swift Testing does not yet support UI test automation.

    NumberPadUITests.swift:
    ```swift
    import XCTest

    final class NumberPadUITests: XCTestCase {
        func testPadButtonMinimumSize() throws {
            XCTFail("not implemented — Wave 0 stub")
        }
        func testPadButtonCount() throws {
            XCTFail("not implemented — Wave 0 stub")
        }
    }
    ```

    GameViewUITests.swift:
    ```swift
    import XCTest

    final class GameViewUITests: XCTestCase {
        func testScoreVisible() throws {
            XCTFail("not implemented — Wave 0 stub")
        }
    }
    ```

    Both files go in the "Math Quiz UITests" target. Do not add @testable import here — UI tests use the app's public surface via XCUIApplication.
  </action>
  <verify>
    <automated>xcodebuild test -scheme "Math Quiz" -destination "platform=iOS Simulator,name=iPhone 16" -only-testing "Math QuizUITests" 2>&1 | grep -E "error:|Build FAILED|Test Suite" | head -20</automated>
  </verify>
  <done>Both UI test files exist and compile. XCTFail stubs make tests red. Build succeeds with no compiler errors.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm stubs compile and are red:
`xcodebuild test -scheme "Math Quiz" -destination "platform=iOS Simulator,name=iPhone 16" 2>&1 | grep -E "error:|Build FAILED|passed|failed" | head -20`

Expected: Build succeeds, 0 passed, all stubs fail. No compiler errors.
</verification>

<success_criteria>
- Six test files exist with correct target membership
- All stubs compile without errors
- All stubs are red (failing, not skipped or erroring at build time)
- No production code created in this plan
- The automated verify commands in plans 01-05 all resolve to existing test files
</success_criteria>

<output>
After completion, create `/Users/adam/dev/Math Quiz/.planning/phases/01-foundation-and-core-game-loop/01-00-SUMMARY.md`
</output>
